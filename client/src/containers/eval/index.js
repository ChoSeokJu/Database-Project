import React from 'react';
import { TopBar } from '../../components';

class Eval extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <TopBar />
      </div>
    );
  }
}

export default Eval;
